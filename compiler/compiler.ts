let channel: string = 'Gaveta'
let subscribers: number = 1313251

// channel = subscribers
console.log(channel)

// let name = 'Pedro'

function sum(a: any, b: any) {
  return a + b
}

let anything
anything = 12
anything = 'abc'

function greet (isMorning: boolean): string {
  let salute: string
  if (isMorning) {
    salute = 'Good morning!'
  } else {
    salute = 'Have a good life!'
  }
  return salute
}